// game display

export const NET = Object.freeze({
    COLOR: 'white',
    WIDTH: 2,
    DASH_SIZE: 12,
    GAP_SIZE: 9,
});

export const CANVAS = Object.freeze({
    ORIGIN_Y: 0,
    ORIGIN_X: 0,
});

export const SCORE_POSITIONS = Object.freeze({
    PLAYER1_Y: 50,
    PLAYER2_Y: 50,
});

export const PLAY_BUTTON = Object.freeze({
    X: 400,
    Y: 300,
});